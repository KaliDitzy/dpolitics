#category = short_term medium_term or long_term
# ROOT is faction leader, FROM is faction member who opens UI and views goals, completed/cancel/complete_effect do not have FROM scopes

faction_goal_unify_germany = {
	name = unify_germany_goal_name
	description = unify_germany_goal_desc
	category = long_term
	group = FOCUS_FILTER_POLITICAL_CHARACTER

	allowed = {
		
	}
	visible = {
		FROM = {
			is_major = yes
			german_capital_state = yes
		}
		NOT = { any_country = { has_country_flag = GER_formed } }
	}
	completed = {
		check_variable = { global.day_1_over = 1 }
		ROOT = {
			has_war = no
			all_allied_Country = {
				has_war = no
			}
		}
		OR = {
			ROOT = {
				faction_influence_ratio > 0.7
			}
			faction_power_projection > 1200
		}
	}
	complete_effect = {
		ROOT = {
			country_event = {
				id = deutscher_bund.1
				days = 4
				random_days = 3
			}
		}
	}
	ai_will_do = {
		base = 1000
	}
}